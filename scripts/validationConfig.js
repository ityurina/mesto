const validationConfig = {
    formSelector: '.popup__form',                 // форма
    inputSelector: '.popup__item',               // инпут
    buttonSelector: '.popup__btn',              // кнопка
    inactiveButtonClass: 'popup__btn_inactive',// неактивная кнопка
    inputErrorClass: 'popup__item_type_err',  // инпут с красным подчеркиванием(ошибка)
    errorClass: 'popup__input-error_active'  // спан с ошибкой
};